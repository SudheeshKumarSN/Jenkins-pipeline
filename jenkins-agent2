pipeline {
    agent {
        label 'slave1'      //here the jenkins will execute the script in slave1 and pipeline level has the highest  priority
    }
    stages{
        stage("checkout"){
            agent {
                label 'salve1'
            }
            steps{
                sh 'echo welcome to jenkins'
            }
        }
        stage("Build"){
            agent{
                label 'slave2'
            }
            steps{
                sh "ls -ltr"
                echo"build completed successfully...."
            }
        }
        stage("Deploy"){
            agent{
                label 'slave2'
                } 
                steps{
                    sh "echo deoployed successfully..."
                }
            }
        }
    }
